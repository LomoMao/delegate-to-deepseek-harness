# Copy this file to .env (git-ignored) if you prefer env-file configuration.
# The DeepSeek Harness process reads these from the environment.
#
# Required for both the MCP bridge and the headless fallback.
DEEPSEEK_API_KEY=
# Optional: only set when pointing at a non-default endpoint.
DEEPSEEK_BASE_URL=
